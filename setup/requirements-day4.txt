# The packages Day 4 needs beyond the base ones.
#
# Install them into your home directory, with the base versions as constraints
# so that pip cannot replace what the image provides. Run it from the
# repository root:
#
#   pip install --user -c setup/requirements.txt -r setup/requirements-day4.txt
#
# Day 4 also needs PyTorch, which the GPU flavour of the Hub already provides.
# Do not install PyTorch yourself. A copy in your home directory takes
# precedence over the image's CUDA build, and the GPU then stops being used
# without anything saying so.
#
# One line is enough. transformers brings huggingface_hub, tokenizers,
# safetensors, and tqdm with it, and the notebooks write their own training
# loops rather than using the Trainer, so datasets, evaluate, and accelerate
# are not needed. Resolved against the base pins, this installs 27 packages and
# replaces none of numpy, pandas, scikit-learn, or PyTorch.
transformers==5.16.1
