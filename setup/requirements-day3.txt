# The packages Day 3 needs beyond the base ones.
#
# Install them into your home directory, with the base versions as constraints
# so that pip cannot replace what the image provides. Run it from the
# repository root:
#
#   pip install --user -c setup/requirements.txt -r setup/requirements-day3.txt
#
# Day 3 also needs PyTorch, which the GPU flavour of the Hub already provides.
# Do not install PyTorch yourself. A copy in your home directory takes
# precedence over the image's CUDA build, and the GPU then stops being used
# without anything saying so.
opencv-python==5.0.0.93
